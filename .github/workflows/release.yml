name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build-release:
    name: Build Release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          # Linux: skip `embeddings` because ort's prebuilt onnxruntime archive
          # references glibc 2.38+ symbols that aren't available on Bookworm/Ubuntu 22.04.
          # Tracked separately for parity; see follow-up issue.
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact_name: muesli
            asset_name: muesli-linux-x86_64
            features: "--no-default-features --features index,summaries,mcp,storage,tui"
          - os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            artifact_name: muesli
            asset_name: muesli-linux-aarch64
            features: "--no-default-features --features index,summaries,mcp,storage,tui"
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: muesli
            asset_name: muesli-macos-aarch64
            features: "--all-features"
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: muesli.exe
            asset_name: muesli-windows-x86_64.exe
            features: "--all-features"

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release ${{ matrix.features }} --target ${{ matrix.target }}

      - name: Strip binary (Unix)
        if: runner.os != 'Windows'
        run: strip target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ./target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

  create-release:
    name: Create Release
    needs: build-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          for dir in artifacts/*/; do
            name=$(basename "$dir")
            if [[ "$name" == *"windows"* ]]; then
              cp "$dir"* "release/${name}"
            else
              cp "$dir"* "release/${name}"
              chmod +x "release/${name}"
            fi
          done
          ls -la release/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true

  publish-crate:
    name: Publish to crates.io
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_TOKEN }}
        continue-on-error: true
